node {
	stage('Prepare Ansible Hosts') {
		checkout scm
		sh "echo ${elkTarget} >> ./ansible/ansible_hosts"
	}

	stage('Install Java 8') {
		sh "/usr/local/bin/ansible-playbook -i ansible/ansible_hosts --keyfile ~/.ssh/id_rsa ansible/roles/java8/tasks/main.yml"
	}
}